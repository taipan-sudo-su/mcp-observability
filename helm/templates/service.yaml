apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-observability.fullname" . }}
  labels:
    {{- include "mcp-observability.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-observability.selectorLabels" . | nindent 4 }}
